name: Issue 5 fixture TDD

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Paused while correcting fixture verification
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fixture generation resumes after the corrected script is committed."
