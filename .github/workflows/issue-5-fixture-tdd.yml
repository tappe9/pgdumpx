name: Issue 5 fixture TDD

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Paused during Green setup
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fixture generation is enabled after the Green inputs are committed."
